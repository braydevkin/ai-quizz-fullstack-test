/**
 * Runs after the test framework is installed, for every test file.
 *
 * `@testing-library/jest-dom` adds the DOM matchers (`toBeInTheDocument`,
 * `toHaveClass`, …). Testing Library unmounts and cleans the DOM between tests
 * on its own, so nothing else is needed here yet.
 */
import '@testing-library/jest-dom'
