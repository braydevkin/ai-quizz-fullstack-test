# `src/hooks`

Reusable React hooks shared across features (`use-media-query`, `use-local-storage`,
…). Hooks used by a single feature live next to it in `src/features/<feature>`.

Empty during the infrastructure phase.
