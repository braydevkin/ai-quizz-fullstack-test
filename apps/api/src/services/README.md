# `src/services`

Integrations with the outside world that are not owned by a single feature
module: mailers, object storage, third-party HTTP clients, cache adapters.

Feature-specific business services belong in `src/modules/<feature>` instead.

Empty during the infrastructure phase.
